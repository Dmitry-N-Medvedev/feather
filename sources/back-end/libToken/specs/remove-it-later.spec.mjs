import mocha from 'mocha';
import chai from 'chai';

const {
  describe,
  it,
} = mocha;
const {
  expect,
} = chai;

describe('remove', () => {
  it('should be removed when proper tests emerge', async () => {
    expect(true).to.be.true;
  });
});
